# File: k8s/redis/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: cfdi-staging
  labels:
    app: redis
spec:
  type: ClusterIP
  selector:
    app: redis
  ports:
    - port: 6379
      targetPort: 6379